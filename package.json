{
  "name": "wanderlust-monorepo",
  "version": "1.0.0",
  "description": "Wanderlust – root workspace (backend + frontend)",
  "private": true,
  "scripts": {
    "install": "cd backend && npm install",
    "build": "cd backend && npm install",
    "start": "node backend/app.js",
    "dev": "cd backend && nodemon app.js"
  }
}
